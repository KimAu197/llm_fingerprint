# Requirements for base family fingerprinting experiment

# Core ML
torch>=2.0.0
transformers>=4.30.0

# Data processing
pandas>=1.5.0

# Visualization
matplotlib>=3.5.0
seaborn>=0.12.0

# Already available from watermarking/utils
# (no additional requirements needed if those are installed)
